{
  "name": "@rhangai/vue-submit-composition",
  "version": "0.1.12",
  "description": "Wrapper to handle requests",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "tsc",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src",
    "prepublishOnly": "yarn run -s clean && yarn run -s build"
  },
  "devDependencies": {
    "@vue/composition-api": "^1.0.0-rc.9",
    "ramda": "^0.27.1",
    "rimraf": "^3.0.2"
  },
  "peerDependencies": {
    "@vue/composition-api": "*",
    "ramda": "^0.27.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
