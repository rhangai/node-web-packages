{
  "name": "@rhangai/vuetify-util",
  "version": "0.2.14",
  "description": "Vuetify utility",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "tsc",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src",
    "preversion": "yarn run -s clean && yarn run -s build"
  },
  "devDependencies": {
    "@rhangai/web-common": "^0.2.6",
    "@vue/composition-api": "^1.0.0-rc.11",
    "ramda": "^0.27.1",
    "rimraf": "^3.0.2"
  },
  "peerDependencies": {
    "@rhangai/web-common": "^0.1.0",
    "@vue/composition-api": "*",
    "ramda": "^0.27.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "imask": "^6.0.7"
  }
}
