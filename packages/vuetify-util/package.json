{
  "name": "@rhangai/vuetify-util",
  "version": "0.4.0",
  "description": "Vuetify utility",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf lib",
    "prebuild": "yarn run -s clean",
    "build": "tsc",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src",
    "preversion": "yarn run -s build"
  },
  "devDependencies": {
    "@rhangai/web-common": "^0.4.0",
    "rimraf": "^3.0.2",
    "vue-demi": "^0.13"
  },
  "peerDependencies": {
    "@rhangai/web-common": "^0.2.6",
    "vue-demi": "*"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "imask": "^6.0.7"
  }
}
